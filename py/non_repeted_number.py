numbers = [1, 2, 3, 4]

# Count of different numbers
different_count = len(numbers)

# Count of non-repeated numbers
non_repeated_count = len(set(numbers))

print("Count of different numbers:", different_count)
print("Count of non-repeated numbers:", non_repeated_count)
